# AGENTS.md

## Git 规则

- **只在本地提交，不要 `git push` 到远端**，除非用户明确要求推送。
- 其他 git 变更操作（commit/rebase 等）仍需先征得用户确认。

## 语言规则

- **Commit message 使用英文描述**（遵循 Conventional Commits：`feat:`/`fix:`/`refactor:`/`docs:`/`test:` 等）。
- **代码注释使用英文**（`//`、`///`、`//!`、`/* */` 等）。
- **文档可以使用中文**（README、`docs/` 下的 design/spec/plan 等）。
