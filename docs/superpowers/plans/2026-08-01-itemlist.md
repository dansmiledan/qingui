# ItemList 实现计划：容器型列表 + 视口裁剪 + 选中态样式

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增容器型列表控件 ItemList（item 为子 UI 树），并落地两个通用基础设施：`CLIP_CHILDREN` 视口裁剪与 `State::SELECTED` 选中态样式。

**Architecture:** 见 `docs/superpowers/specs/2026-08-01-itemlist-design.md`。ItemList = 视口节点（CLIP_CHILDREN）+ content 容器（Flex column，translate.y 滚动）+ item 容器（用户搭内容）；选中态 = `State::SELECTED` + `style_selected` 叠加（pressed > focused > selected），零自绘。

**Tech Stack:** Rust, `#![no_std]` + `alloc`；host 端集成测试（RecFlush 像素断言）。

## Global Constraints

- **no_std**：`qingui/src/` 内禁止 `std`，只用 `core`/`alloc`；不新增依赖。
- **注释用中文**，风格与现有代码一致。
- **Git 规则（AGENTS.md）**：commit 前需用户确认；**永不 push**。
- 所有命令在仓库根 `/Users/yintan/Documents/workspace/project/rust-lvgl` 执行；测试命令 `cargo test -p qingui`（当前 147 全过，每个 Task 结束必须保持全绿）。
- 公开 API 是 **Ui 中心风格**（`ui.xxx(obj, ...)`）；`grep -n "WidgetKind::" qingui/src/ui.rs` 不得新增变体分支（经 `as_itemlist` 访问器）。
- 不改文本 `List` 的任何行为与 API；list_fx/list_nav 等既有测试零改动通过。
- 测试断言的数值必须来自实现/布局的真实计算，不得硬编猜值。

## 关键实现要点（各 Task 引用）

- **透明容器**：`Style::default()` 解析后 `bg_opa = 255` + 黑底（不透明）。透明容器必须 `bg_opa = Some(0)`（demo.rs 的 `transparent()` 即此模式）。
- **resolve 回落**：`Style` overlay 某字段为 `None` 时回落到 base 的同名字段。所以默认选中样式必须显式带 `bg_opa(255)`，否则 item 基底的 `bg_opa(0)` 会让选中背景不可见。
- **content/item 的 sizing 走 Style 字段**：`sizing_w: Some(Sizing::GROW)` 直接写进 `set_style` 的 Style 即可（布局读 resolved style）。
- **测试可见性**：`ItemListState.content` 是 `pub(crate)`，集成测试（外部 crate）不可见——滚动断言用公开 API `ui.abs_rect(item)` 或像素断言，不读 content。

---

### Task 1: CLIP_CHILDREN 视口裁剪

**Files:**
- Modify: `qingui/src/node.rs`（Flag 加位）
- Modify: `qingui/src/ui.rs`（`set_clip_children` + `draw_node` 裁剪分支，约 717-752 行区域）
- Test: `qingui/tests/clip.rs`（新建）

**Interfaces:**
- Consumes: 现有 `Flag`（node.rs）、`Ui::draw_node`、`ObjBuilder`（widgets/obj.rs）。
- Produces:
  - `Flag::CLIP_CHILDREN = 1 << 3`
  - `Ui::set_clip_children(&mut self, obj: ObjRef, clip: bool)`（Task 3 的 ItemList Builder 依赖）

- [ ] **Step 1: 写失败测试**

新建 `qingui/tests/clip.rs`：

```rust
use qingui::display::Flush;
use qingui::widgets::obj::ObjBuilder;
use qingui::{Color, Rect, Ui};
use std::cell::RefCell;
use std::rc::Rc;

#[derive(Default)]
struct RecFlush {
    chunks: Vec<(Rect, Vec<Color>)>,
}
struct SharedFlush(Rc<RefCell<RecFlush>>);
impl Flush for SharedFlush {
    fn flush(&mut self, area: Rect, pixels: &[Color]) {
        self.0.borrow_mut().chunks.push((area, pixels.to_vec()));
    }
}
fn px(rec: &Rc<RefCell<RecFlush>>, x: i32, y: i32) -> Color {
    let chunks = &rec.borrow().chunks;
    for (area, buf) in chunks.iter().rev() {
        if x >= area.x && x < area.right() && y >= area.y && y < area.bottom() {
            return buf[((y - area.y) * area.w + (x - area.x)) as usize];
        }
    }
    panic!("pixel not flushed");
}

fn build(bg: Color) -> (Rc<RefCell<RecFlush>>, Ui, qingui::ObjRef, qingui::ObjRef) {
    let rec = Rc::new(RefCell::new(RecFlush::default()));
    let mut ui = Ui::new(64, 64, 16);
    ui.set_flush(Box::new(SharedFlush(rec.clone())));
    let mut s = qingui::style::Style::default();
    s.bg_color = Some(bg);
    ui.set_style(ui.screen(), s);
    // 视口 20x20 @ (5,5)
    let vp = ObjBuilder::new().size(20, 20).build(&mut ui, ui.screen());
    ui.set_pos(vp, 5, 5);
    // 白色子块 20x20，放在 vp 内 (10,0)：右半超出视口
    let child = ObjBuilder::new()
        .size(20, 20)
        .style(qingui::style::Style::new().bg(Color::WHITE))
        .build(&mut ui, vp);
    ui.set_pos(child, 10, 0);
    (rec, ui, vp, child)
}

#[test]
fn clip_children_cuts_descendant_at_parent_edge() {
    let (rec, mut ui, vp, _child) = build(Color::BLACK);
    ui.set_clip_children(vp, true);
    ui.render();
    assert_eq!(px(&rec, 15, 10), Color::WHITE); // 视口内部分正常绘制
    assert_eq!(px(&rec, 26, 10), Color::BLACK); // 超出视口部分被裁
}

#[test]
fn no_clip_children_draws_beyond_parent() {
    let (rec, mut ui, _vp, _child) = build(Color::BLACK);
    ui.render();
    assert_eq!(px(&rec, 15, 10), Color::WHITE);
    assert_eq!(px(&rec, 26, 10), Color::WHITE); // 默认不裁剪：子对象可画出父边界
}
```

Run: `cargo test -p qingui --test clip`
Expected: 编译失败（`set_clip_children` 不存在）。

- [ ] **Step 2: Flag 加位 + set_clip_children**

`qingui/src/node.rs` 的 `Flag` 中追加：

```rust
        /// 视口裁剪：子树绘制被裁剪到本对象矩形内（滚动容器用）
        const CLIP_CHILDREN = 1 << 3;
```

`qingui/src/ui.rs`（放在 `set_ignore_layout` 附近）：

```rust
    /// 设置视口裁剪：子树绘制被裁剪到本对象矩形内（对齐 LVGL 的 clip 内容，滚动容器用）
    pub fn set_clip_children(&mut self, obj: ObjRef, clip: bool) {
        if let Some(n) = self.arena.get_mut(obj) {
            n.flags.set(Flag::CLIP_CHILDREN, clip);
        }
        self.invalidate_obj(obj);
    }
```

- [ ] **Step 3: draw_node 裁剪分支**

`qingui/src/ui.rs` 的 `draw_node` 末尾，把：

```rust
        for c in self.children_z_sorted(obj) {
            self.draw_node(c, clip, len);
        }
```

替换为：

```rust
        // 视口裁剪：子树 clip 收缩到本对象矩形内；不相交则整棵子树跳过
        let child_clip = if flags.contains(Flag::CLIP_CHILDREN) {
            match clip.intersect(&abs) {
                Some(c) => c,
                None => return,
            }
        } else {
            clip
        };
        for c in self.children_z_sorted(obj) {
            self.draw_node(c, child_clip, len);
        }
```

注意：早退 `None => return` 只在 CLIP_CHILDREN 分支；默认（不裁剪）父节点即使与脏区不相交也照常递归子节点（保持现状行为）。

- [ ] **Step 4: 验证**

Run: `cargo test -p qingui`
Expected: 全绿（147 既有 + clip.rs 2 个新 = 149）。

- [ ] **Step 5: Commit（先获用户确认）**

```bash
git add qingui/src qingui/tests/clip.rs
git commit -m "feat: CLIP_CHILDREN viewport clipping for scrolling containers"
```

---

### Task 2: State::SELECTED + style_selected

**Files:**
- Modify: `qingui/src/node.rs`（State 加位 + Node 加字段）
- Modify: `qingui/src/ui.rs`（`set_style_selected` + `resolved_style` 叠加链，约 282-295 行区域）
- Test: `qingui/tests/selected.rs`（新建）

**Interfaces:**
- Consumes: `State`/`Node`（node.rs）、`resolve`（style.rs:163，overlay 为 `Option<&Style>`，单一层）。
- Produces:
  - `State::SELECTED = 1 << 4`
  - `Node { pub style_selected: Style, .. }`
  - `Ui::set_style_selected(&mut self, obj: ObjRef, style: Style)`
  - `resolved_style` 叠加顺序：pressed > focused > selected（Task 3 依赖）

- [ ] **Step 1: 写失败测试**

新建 `qingui/tests/selected.rs`：

```rust
use qingui::display::Flush;
use qingui::node::State;
use qingui::style::Style;
use qingui::widgets::obj::ObjBuilder;
use qingui::{Color, Rect, Ui};
use std::cell::RefCell;
use std::rc::Rc;

#[derive(Default)]
struct RecFlush {
    chunks: Vec<(Rect, Vec<Color>)>,
}
struct SharedFlush(Rc<RefCell<RecFlush>>);
impl Flush for SharedFlush {
    fn flush(&mut self, area: Rect, pixels: &[Color]) {
        self.0.borrow_mut().chunks.push((area, pixels.to_vec()));
    }
}
fn px(rec: &Rc<RefCell<RecFlush>>, x: i32, y: i32) -> Color {
    let chunks = &rec.borrow().chunks;
    for (area, buf) in chunks.iter().rev() {
        if x >= area.x && x < area.right() && y >= area.y && y < area.bottom() {
            return buf[((y - area.y) * area.w + (x - area.x)) as usize];
        }
    }
    panic!("pixel not flushed");
}

fn build() -> (Rc<RefCell<RecFlush>>, Ui, qingui::ObjRef) {
    let rec = Rc::new(RefCell::new(RecFlush::default()));
    let mut ui = Ui::new(64, 64, 16);
    ui.set_flush(Box::new(SharedFlush(rec.clone())));
    let o = ObjBuilder::new()
        .size(10, 10)
        .style(Style::new().bg(Color::RED))
        .build(&mut ui, ui.screen());
    (rec, ui, o)
}

#[test]
fn selected_state_applies_style_selected() {
    let (rec, mut ui, o) = build();
    ui.set_style_selected(o, Style::new().bg(Color::BLUE));
    ui.render();
    assert_eq!(px(&rec, 1, 1), Color::RED); // 未选中：基础样式
    ui.set_state(o, State::SELECTED, true);
    ui.render();
    assert_eq!(px(&rec, 1, 1), Color::BLUE); // 选中：selected 叠加
    ui.set_state(o, State::SELECTED, false);
    ui.render();
    assert_eq!(px(&rec, 1, 1), Color::RED); // 取消选中：还原
}

#[test]
fn overlay_priority_pressed_over_focused_over_selected() {
    let (rec, mut ui, o) = build();
    ui.set_style_selected(o, Style::new().bg(Color::BLUE));
    ui.set_style_focused(o, Style::new().bg(Color::GREEN));
    ui.set_style_pressed(o, Style::new().bg(Color::WHITE));
    // selected < focused
    ui.set_state(o, State::SELECTED, true);
    ui.set_state(o, State::FOCUSED, true);
    ui.render();
    assert_eq!(px(&rec, 1, 1), Color::GREEN);
    // pressed 最高
    ui.set_state(o, State::PRESSED, true);
    ui.render();
    assert_eq!(px(&rec, 1, 1), Color::WHITE);
}
```

Run: `cargo test -p qingui --test selected`
Expected: 编译失败（`set_style_selected`/`State::SELECTED` 不存在）。

- [ ] **Step 2: node.rs 加位加字段**

`State` 中追加 `const SELECTED = 1 << 4;`。`Node` 在 `style_focused` 之后加字段 `pub style_selected: crate::style::Style,`，`Node::new` 中对应初始化 `style_selected: crate::style::Style::default(),`。

- [ ] **Step 3: set_style_selected + resolved_style 链**

`qingui/src/ui.rs`（放在 `set_style_focused` 之后，与 set_style_focused 同构）：

```rust
    pub fn set_style_selected(&mut self, obj: ObjRef, style: crate::style::Style) {
        if let Some(n) = self.arena.get_mut(obj) {
            n.style_selected = style;
        }
        self.invalidate_obj(obj);
        self.layout_dirty = true;
    }
```

`resolved_style` 的 overlay 选择替换为：

```rust
        // pressed > focused > selected（互斥取一）
        let overlay = if n.state.contains(State::PRESSED) {
            Some(&n.style_pressed)
        } else if n.state.contains(State::FOCUSED) {
            Some(&n.style_focused)
        } else if n.state.contains(State::SELECTED) {
            Some(&n.style_selected)
        } else {
            None
        };
```

- [ ] **Step 4: 验证**

Run: `cargo test -p qingui`
Expected: 全绿（149 + selected.rs 2 个新 = 151）。

- [ ] **Step 5: Commit（先获用户确认）**

```bash
git add qingui/src qingui/tests/selected.rs
git commit -m "feat: State::SELECTED with style_selected overlay"
```

---

### Task 3: ItemList 控件

**Files:**
- Create: `qingui/src/widgets/itemlist.rs`
- Modify: `qingui/src/widgets/mod.rs`（`pub mod itemlist;` + 变体 + 委托臂 + as_itemlist 访问器）
- Modify: `qingui/src/ui.rs`（`KeyOutcome::NavSelect` 的 apply 臂 + 5 个 `itemlist_*` 方法 + `itemlist_ensure_visible` 内部方法）
- Test: `qingui/tests/itemlist.rs`（新建）

**Interfaces:**
- Consumes: `set_clip_children`（Task 1）、`set_style_selected`/`State::SELECTED`（Task 2）、`KeyOutcome`（widgets/mod.rs，本任务新增 `NavSelect(i32)` 变体）、`ObjBuilder` 同款透明容器模式、`Sizing::GROW`、`Layout::Flex(Flex{ dir: FlexDir::Column, wrap: false, main/cross/track: Align::Start, gap: 0 })`。
- Produces:
  - `WidgetKind::ItemList(itemlist::ItemListState)`；`ItemListState { pub selected: usize, pub(crate) content: ObjRef, pub(crate) sel_style: Style }`
  - `WidgetKind::as_itemlist() -> Option<&ItemListState>` / `as_itemlist_mut()`（pub）
  - `KeyOutcome::NavSelect(i32)`（pub(crate)）
  - `Ui::itemlist_add_item(&mut self, il: ObjRef) -> Option<ObjRef>`（非 ItemList 返回 None）
  - `Ui::itemlist_remove_selected(&mut self, il: ObjRef) -> bool`
  - `Ui::itemlist_select(&mut self, il: ObjRef, idx: usize)`
  - `Ui::itemlist_selected(&self, il: ObjRef) -> usize`
  - `Ui::itemlist_len(&self, il: ObjRef) -> usize`
  - `ItemListBuilder`（`new/size/style/style_selected/sizing/transition/on/build`）

**两处对 spec 的有意偏离（评审时请确认，不算 spec 违规）：**
1. `itemlist_add_item` 返回 `Option<ObjRef>`（spec 写的是 `-> ObjRef`）：误用（非 ItemList）时无法构造合理返回值，`Option` 让误用立即在 `unwrap`/`expect` 处暴露，与库内"查询返回 Option"的惯例一致。
2. `WidgetKind::set_value` 不接入 ItemList（落 `_ => false`）：`set_value(&mut self)` 拿不到 `&Ui` 无法数 content 的子节点做 clamp，在状态里冗余存 len 又会因用户直接 `ui.delete(item)` 而漂移。`value()` 仍返回 `selected as i32`。

- [ ] **Step 1: 写失败测试**

新建 `qingui/tests/itemlist.rs`：

```rust
use qingui::display::Flush;
use qingui::input::Key;
use qingui::node::State;
use qingui::style::Style;
use qingui::widgets::itemlist::ItemListBuilder;
use qingui::widgets::label::LabelBuilder;
use qingui::{Color, EventKind, ObjRef, Rect, Ui};
use std::cell::{Cell, RefCell};
use std::rc::Rc;

#[derive(Default)]
struct RecFlush {
    chunks: Vec<(Rect, Vec<Color>)>,
}
struct SharedFlush(Rc<RefCell<RecFlush>>);
impl Flush for SharedFlush {
    fn flush(&mut self, area: Rect, pixels: &[Color]) {
        self.0.borrow_mut().chunks.push((area, pixels.to_vec()));
    }
}
fn px(rec: &Rc<RefCell<RecFlush>>, x: i32, y: i32) -> Color {
    let chunks = &rec.borrow().chunks;
    for (area, buf) in chunks.iter().rev() {
        if x >= area.x && x < area.right() && y >= area.y && y < area.bottom() {
            return buf[((y - area.y) * area.w + (x - area.x)) as usize];
        }
    }
    panic!("pixel not flushed");
}

/// 建 60x40 视口 + 4 个 item（各 20 高：Label 8px + 上下 pad）
fn build4() -> (Ui, ObjRef, Vec<ObjRef>) {
    let mut ui = Ui::new(160, 120, 120);
    let il = ItemListBuilder::new().size(60, 40).build(&mut ui, ui.screen());
    let mut items = Vec::new();
    for t in ["a", "b", "c", "d"] {
        let it = ui.itemlist_add_item(il).expect("add_item on ItemList");
        LabelBuilder::new(t).build(&mut ui, it);
        ui.set_size(it, 60, 20);
        items.push(it);
    }
    // 布局：ItemList 在 screen 上，默认无 flex → 手动布局
    ui.set_pos(il, 0, 0);
    (ui, il, items)
}

#[test]
fn add_items_and_initial_selection() {
    let (ui, il, items) = build4();
    assert_eq!(ui.itemlist_len(il), 4);
    assert_eq!(ui.itemlist_selected(il), 0);
    assert!(ui.state(items[0]).contains(State::SELECTED)); // 首项自动选中
    assert!(!ui.state(items[1]).contains(State::SELECTED));
}

#[test]
fn select_moves_selected_state_and_fires_value_changed() {
    let (mut ui, il, items) = build4();
    let hits = Rc::new(Cell::new(0));
    let h = hits.clone();
    ui.add_event_cb(il, EventKind::ValueChanged, Box::new(move |_, _, _| h.set(h.get() + 1)));
    ui.itemlist_select(il, 2);
    assert_eq!(ui.itemlist_selected(il), 2);
    assert!(!ui.state(items[0]).contains(State::SELECTED));
    assert!(ui.state(items[2]).contains(State::SELECTED));
    assert_eq!(hits.get(), 1);
    ui.itemlist_select(il, 2); // 无变化：不重发事件
    assert_eq!(hits.get(), 1);
    assert_eq!(ui.value(il), 2); // value() 接入
}

#[test]
fn keyboard_nav_wraps_and_consumes() {
    let (mut ui, il, _items) = build4();
    ui.group_add(il);
    ui.keypad_input(Key::Up); // 循环：0 → 3
    assert_eq!(ui.itemlist_selected(il), 3);
    ui.keypad_input(Key::Down); // 循环：3 → 0
    assert_eq!(ui.itemlist_selected(il), 0);
}

#[test]
fn ensure_visible_scrolls_content() {
    let (mut ui, il, items) = build4();
    ui.itemlist_select(il, 3); // item3 在视口（40 高）之外 → 滚动
    // 滚动后 item3 底对齐视口底：item3 abs y = 40 - 20 = 20
    assert_eq!(ui.abs_rect(items[3]).y, 20);
    assert_eq!(ui.abs_rect(items[0]).y, -40); // item0 被滚出视口上方
}

#[test]
fn viewport_clips_scrolled_items() {
    let rec = Rc::new(RefCell::new(RecFlush::default()));
    let mut ui = Ui::new(160, 120, 120);
    ui.set_flush(Box::new(SharedFlush(rec.clone())));
    let mut ss = Style::default();
    ss.bg_color = Some(Color::BLACK);
    ui.set_style(ui.screen(), ss);
    let il = ItemListBuilder::new().size(60, 40).build(&mut ui, ui.screen());
    ui.set_pos(il, 10, 30); // 视口避开屏幕边缘，滚出区域仍是屏内可断言区
    for _ in 0..4 {
        let it = ui.itemlist_add_item(il).unwrap();
        // 每项整块白色背景，便于像素断言
        ui.set_style(it, Style::new().bg(Color::WHITE));
        ui.set_size(it, 60, 20);
    }
    ui.itemlist_select(il, 3); // 滚动 40px：item2 → y 30..50，item3 → y 50..70
    ui.render();
    assert_eq!(px(&rec, 15, 35), Color::WHITE); // item2 可见
    assert_eq!(px(&rec, 15, 55), Color::WHITE); // item3 可见
    assert_eq!(px(&rec, 15, 25), Color::BLACK); // item1（abs y 10..30）滚出视口上方：被裁
    assert_eq!(px(&rec, 15, 5), Color::BLACK);  // item0（abs y -10..10）滚出视口上方：被裁
}

#[test]
fn remove_selected_clamps_and_reselects() {
    let (mut ui, il, items) = build4();
    ui.itemlist_select(il, 3);
    assert!(ui.itemlist_remove_selected(il)); // 删 item3
    assert_eq!(ui.itemlist_len(il), 3);
    assert_eq!(ui.itemlist_selected(il), 2); // 收敛到末项
    assert!(ui.state(items[2]).contains(State::SELECTED)); // 新选中项置位
}

#[test]
fn empty_list_key_does_not_panic_and_consumes() {
    let mut ui = Ui::new(160, 120, 120);
    let il = ItemListBuilder::new().size(60, 40).build(&mut ui, ui.screen());
    ui.group_add(il);
    ui.keypad_input(Key::Up); // 空列表：不 panic，按键被消费（不移焦）
    assert_eq!(ui.focused(), Some(il));
    assert!(!ui.itemlist_remove_selected(il));
}
```

Run: `cargo test -p qingui --test itemlist`
Expected: 编译失败（`ItemListBuilder`/`itemlist_*` 不存在）。

- [ ] **Step 2: itemlist.rs 骨架（State + on_key）**

新建 `qingui/src/widgets/itemlist.rs`：

```rust
use alloc::vec::Vec;

use crate::arena::ObjRef;
use crate::geometry::{Color, Rect};
use crate::input::Key;
use crate::layout::{Align, Flex, FlexDir, Sizing};
use crate::node::State;
use crate::style::{Layout, Style};
use crate::ui::Ui;
use super::{KeyCtx, KeyOutcome, WidgetKind};

/// 容器型列表：item 为普通子节点（用户自由搭建内容），控件只管选中/导航/滚动。
/// 结构：ItemList（视口，CLIP_CHILDREN）> content（Flex column，translate 滚动）> items
pub struct ItemListState {
    pub selected: usize,
    pub(crate) content: ObjRef,
    pub(crate) sel_style: Style,
}

impl ItemListState {
    pub(crate) fn on_key(&mut self, key: Key, _ctx: KeyCtx) -> KeyOutcome {
        match key {
            // 导航细节需要 Ui（子节点/滚动/事件），由 apply_key_outcome 执行
            Key::Up => KeyOutcome::NavSelect(-1),
            Key::Down => KeyOutcome::NavSelect(1),
            _ => KeyOutcome::Pass,
        }
    }
}

/// 透明容器样式（只做布局/滚动，不画背景）
fn transparent() -> Style {
    let mut s = Style::default();
    s.bg_opa = Some(0);
    s
}

fn column_layout() -> Layout {
    Layout::Flex(Flex {
        dir: FlexDir::Column,
        wrap: false,
        main: Align::Start,
        cross: Align::Start,
        track: Align::Start,
        gap: 0,
    })
}
```

- [ ] **Step 3: mod.rs 接入**

`pub mod itemlist;`（字母序在 `label` 与 `list` 之间——注意 `itemlist` 字母序在 `label` 前）。enum 加变体：

```rust
    ItemList(itemlist::ItemListState),
```

委托臂：

```rust
// draw:    WidgetKind::ItemList(_) => {} // 容器：内容由子节点绘制
// value:   WidgetKind::ItemList(s) => s.selected as i32,
// on_key:  WidgetKind::ItemList(s) => s.on_key(key, ctx),
// tick/set_value/set_range/overflow: 落入既有 _ 臂
```

`KeyOutcome` 加变体（含注释：`/// 列表型控件移动选中（步进 ±1），由 Ui 执行（需要子节点/滚动/事件）`）：

```rust
    NavSelect(i32),
```

访问器（与既有 14 组同构，pub）：

```rust
    pub fn as_itemlist(&self) -> Option<&itemlist::ItemListState> {
        match self { WidgetKind::ItemList(s) => Some(s), _ => None }
    }
    pub fn as_itemlist_mut(&mut self) -> Option<&mut itemlist::ItemListState> {
        match self { WidgetKind::ItemList(s) => Some(s), _ => None }
    }
```

- [ ] **Step 4: Ui 方法 + apply_key_outcome 臂**

`qingui/src/ui.rs` 的 `apply_key_outcome` 加臂：

```rust
            KeyOutcome::NavSelect(d) => {
                // 列表型控件移动选中（空列表也消费按键，对齐文本 List）
                let n = self.itemlist_len(obj);
                if n > 0 {
                    let cur = self.itemlist_selected(obj);
                    let next = (cur as i32 + d).rem_euclid(n as i32) as usize;
                    self.itemlist_select(obj, next);
                }
                true
            }
```

5 个 `itemlist_*` 方法 + 内部 `itemlist_ensure_visible`（放在 `list_len` 附近）：

```rust
    /// 向 ItemList 追加一个 item 容器（Obj，宽 GROW，透明背景，带 SELECTED 样式），
    /// 返回该容器（用户往里搭建内容）；il 非 ItemList 时返回 None
    pub fn itemlist_add_item(&mut self, il: ObjRef) -> Option<ObjRef> {
        let (content, sel_style, was_empty) = {
            let s = self.arena.get(il)?.kind.as_itemlist()?;
            (s.content, s.sel_style.clone(), self.children(s.content).is_empty())
        };
        let item = self.insert_node(content, Rect::default(), WidgetKind::Obj);
        let mut st = crate::widgets::itemlist::item_base_style();
        st.sizing_w = Some(crate::layout::Sizing::GROW);
        self.set_style(item, st);
        self.set_style_selected(item, sel_style);
        // 首项自动选中
        if was_empty {
            self.set_state(item, State::SELECTED, true);
        }
        Some(item)
    }

    /// 删除 ItemList 的选中 item（空列表返回 false），selected 收敛并把选中位移给相邻项
    pub fn itemlist_remove_selected(&mut self, il: ObjRef) -> bool {
        let Some((content, selected)) = self
            .arena
            .get(il)
            .and_then(|n| n.kind.as_itemlist())
            .map(|s| (s.content, s.selected))
        else {
            return false;
        };
        let kids = self.children(content);
        if kids.is_empty() || selected >= kids.len() {
            return false;
        }
        self.delete(kids[selected]);
        let new_len = kids.len() - 1;
        let new_sel = if new_len == 0 { 0 } else { selected.min(new_len - 1) };
        if let Some(s) = self.arena.get_mut(il).and_then(|n| n.kind.as_itemlist_mut()) {
            s.selected = new_sel;
        }
        // 选中位移给相邻项（删除中间项 → 原下一项；删除末项 → 原上一项）
        if new_len > 0 {
            let target = if selected < new_len { kids[selected + 1] } else { kids[selected - 1] };
            self.set_state(target, State::SELECTED, true);
        }
        self.itemlist_ensure_visible(il);
        true
    }

    /// 选中 ItemList 第 idx 项（clamp 到合法范围）；变化才切换并发 ValueChanged
    pub fn itemlist_select(&mut self, il: ObjRef, idx: usize) {
        let Some((content, cur)) = self
            .arena
            .get(il)
            .and_then(|n| n.kind.as_itemlist())
            .map(|s| (s.content, s.selected))
        else {
            return;
        };
        let kids = self.children(content);
        if kids.is_empty() {
            return;
        }
        let nidx = idx.min(kids.len() - 1);
        if nidx == cur {
            return;
        }
        self.set_state(kids[cur], State::SELECTED, false);
        self.set_state(kids[nidx], State::SELECTED, true);
        if let Some(s) = self.arena.get_mut(il).and_then(|n| n.kind.as_itemlist_mut()) {
            s.selected = nidx;
        }
        self.itemlist_ensure_visible(il);
        self.send_event(il, crate::event::EventKind::ValueChanged);
    }

    pub fn itemlist_selected(&self, il: ObjRef) -> usize {
        self.arena
            .get(il)
            .and_then(|n| n.kind.as_itemlist())
            .map(|s| s.selected)
            .unwrap_or(0)
    }

    pub fn itemlist_len(&self, il: ObjRef) -> usize {
        self.arena
            .get(il)
            .and_then(|n| n.kind.as_itemlist())
            .map(|s| self.children(s.content).len())
            .unwrap_or(0)
    }

    /// 滚动 content（translate.y）使选中 item 在视口内可见（瞬时，无动画）
    fn itemlist_ensure_visible(&mut self, il: ObjRef) {
        let Some((content, selected)) = self
            .arena
            .get(il)
            .and_then(|n| n.kind.as_itemlist())
            .map(|s| (s.content, s.selected))
        else {
            return;
        };
        let Some(item) = self.arena.get(content).and_then(|c| c.children.get(selected).copied()) else {
            return;
        };
        let vp_h = self.rect(il).h;
        let content_h = self.rect(content).h;
        let ir = self.rect(item); // 相对 content 的本地 rect
        let off = self.translate(content).y;
        let mut new_off = if content_h <= vp_h {
            0 // 内容不足一屏：不滚动
        } else if ir.h >= vp_h {
            -ir.y // item 高于视口：顶对齐
        } else {
            let top = ir.y + off;
            let bottom = top + ir.h;
            if top < 0 {
                off - top // 上滚：顶对齐
            } else if bottom > vp_h {
                off - (bottom - vp_h) // 下滚：底对齐
            } else {
                off
            }
        };
        new_off = new_off.min(0); // 不允许向下露出内容顶部空白
        if new_off != off {
            self.set_translate(content, 0, new_off);
        }
    }
```

`item_base_style`（itemlist.rs，pub(crate)，供 add_item 使用）：

```rust
/// item 容器的基础样式：透明背景（SELECTED 时由 style_selected 叠加高亮）
pub(crate) fn item_base_style() -> Style {
    transparent()
}
```

- [ ] **Step 5: ItemListBuilder**

追加到 `itemlist.rs`（形态对齐现有 Builder）：

```rust
/// ItemList 构建器：默认 120x100，theme_itemlist 视口样式 + 默认选中样式（蓝底）
pub struct ItemListBuilder {
    size: Option<(i32, i32)>,
    style: Option<Style>,
    style_selected: Option<Style>,
    sizing: Option<(Option<Sizing>, Option<Sizing>)>,
    transition: Option<(u32, crate::anim::Easing)>,
    events: Vec<(crate::event::EventKind, crate::event::EventCb)>,
}

impl ItemListBuilder {
    pub fn new() -> Self {
        Self { size: None, style: None, style_selected: None, sizing: None, transition: None, events: Vec::new() }
    }
    pub fn size(mut self, w: i32, h: i32) -> Self { self.size = Some((w, h)); self }
    pub fn style(mut self, s: Style) -> Self { self.style = Some(s); self }
    /// item 的选中样式（叠加于 State::SELECTED）。
    /// 注意：必须显式含 bg_opa，否则 item 基底的 bg_opa(0) 会让高亮不可见
    pub fn style_selected(mut self, s: Style) -> Self { self.style_selected = Some(s); self }
    pub fn sizing(mut self, w: Option<Sizing>, h: Option<Sizing>) -> Self { self.sizing = Some((w, h)); self }
    pub fn transition(mut self, dur: u32, easing: crate::anim::Easing) -> Self { self.transition = Some((dur, easing)); self }
    pub fn on(mut self, kind: crate::event::EventKind, cb: crate::event::EventCb) -> Self {
        self.events.push((kind, cb)); self
    }

    pub fn build(self, ui: &mut Ui, parent: ObjRef) -> ObjRef {
        let (w, h) = self.size.unwrap_or((120, 100));
        // 视口节点先以 Obj 占位（content 引用需要自指后的句柄）
        let r = ui.insert_node(parent, Rect::new(0, 0, w, h), WidgetKind::Obj);
        ui.set_clip_children(r, true);
        // content：Flex column 容器，宽 GROW，透明背景
        let content = ui.insert_node(r, Rect::new(0, 0, w, 0), WidgetKind::Obj);
        ui.set_style(content, transparent());
        ui.set_sizing(content, Some(Sizing::GROW), None);
        ui.set_layout(content, column_layout());
        // 占位 kind 换真身
        let sel_style = self.style_selected.unwrap_or_else(default_sel_style);
        if let Some(n) = ui.arena.get_mut(r) {
            n.kind = WidgetKind::ItemList(ItemListState { selected: 0, content, sel_style });
        }
        // 视口样式（默认对齐 theme_list 的深色底 + 边框）
        let mut vs = self.style.unwrap_or_else(|| {
            let mut s = Style::default();
            s.bg_color = Some(Color::rgb(34, 34, 44));
            s.bg_opa = Some(255);
            s.border_color = Some(Color::rgb(70, 70, 90));
            s.border_width = Some(1);
            s
        });
        ui.set_style(r, {
            if vs.bg_opa.is_none() { vs.bg_opa = Some(255); }
            vs
        });
        if let Some((sw, sh)) = self.sizing {
            ui.set_sizing(r, sw, sh);
        }
        if let Some(t) = self.transition {
            ui.set_transition(r, Some(t));
        }
        for (k, cb) in self.events {
            ui.add_event_cb(r, k, cb);
        }
        r
    }
}

/// 默认选中样式（对齐文本 List 高亮色 rgb(50,70,120)；必须显式 bg_opa(255)）
fn default_sel_style() -> Style {
    let mut s = Style::default();
    s.bg_color = Some(Color::rgb(50, 70, 120));
    s.bg_opa = Some(255);
    s
}
```

- [ ] **Step 6: 验证**

Run: `cargo test -p qingui`
Expected: 全绿（151 + itemlist.rs 7 个新 = 158）。`grep -n "WidgetKind::" qingui/src/ui.rs` 不新增变体分支。

- [ ] **Step 7: Commit（先获用户确认）**

```bash
git add qingui/src qingui/tests/itemlist.rs
git commit -m "feat: ItemList container widget with subtree items"
```

---

### Task 4: 示例 + 验收

**Files:**
- Modify: `qingui/examples/gallery.rs`（新增 ItemList 展示区）
- Test: 无新测试（复用 Task 1-3 的测试 + 示例编译）

**Interfaces:**
- Consumes: Task 1-3 全部公开 API。
- Produces: 无新接口。

- [ ] **Step 1: gallery 增加 ItemList 示例**

读 `qingui/examples/gallery.rs` 现有结构，在不破坏既有布局的前提下新增一个 ItemList 展示区（新 section 或替换一个既有小部件区域）：

```rust
// 菜单型 ItemList：每项 Led 图标 + Label 文字
let menu = ItemListBuilder::new().size(140, 60).build(&mut ui, screen);
for (color, name) in [
    (Color::GREEN, "Wi-Fi"),
    (Color::BLUE, "Bluetooth"),
    (Color::RED, "Airplane"),
    (Color::YELLOW, "Location"),
    (Color::WHITE, "About"),
] {
    let it = ui.itemlist_add_item(menu).unwrap();
    ui.set_layout(it, Layout::Flex(Flex { dir: FlexDir::Row, wrap: false, main: Align::Start, cross: Align::Center, track: Align::Start, gap: 6 }));
    LedBuilder::new(color).size(8, 8).build(&mut ui, it);
    LabelBuilder::new(name).build(&mut ui, it);
    ui.set_size(it, 140, 16);
}
ui.group_add(menu);
```

布局参数按 gallery 实际剩余空间微调；若页面空间不足，替换掉一个既有次要部件区域（保持示例整体协调）。Run: `cargo check -p qingui --examples` 通过。

- [ ] **Step 2: 全量验收**

```bash
cargo test -p qingui                                    # 全绿（158）
cargo check -p qingui --examples                        # 通过
cargo build -p qingui --target thumbv7em-none-eabihf    # 通过
grep -c "WidgetKind::" qingui/src/ui.rs                 # 与 Task 3 前一致（仅 3 处既有合规引用）
grep -rn "tick_list_fx\|debug_kind" qingui/src          # 无输出（既有验收不破）
```

- [ ] **Step 3: Commit（先获用户确认）**

```bash
git add qingui/examples/gallery.rs
git commit -m "examples: ItemList menu demo in gallery"
```

---

## Self-Review 记录

- **Spec 覆盖**：CLIP_CHILDREN（Task 1）✓；SELECTED + style_selected + 叠加链（Task 2）✓；ItemList 三层结构/ItemListState/on_key/Builder/5 个 Ui 方法（Task 3）✓；value 接入（value() → selected）✓；事件语义（select/导航发 ValueChanged，Enter 经默认 Pass → Clicked）✓；非目标（无增删动画/无虚拟化/文本 List 不动）✓——计划未引入这些功能；测试覆盖 spec 第 6 节全部条目 ✓。
- **有意偏离 spec 两处**（已在 Task 3 开头声明）：`itemlist_add_item` 返回 `Option<ObjRef>`；`set_value` 不接入 ItemList（`value()` 仍接入）。
- **Placeholder 扫描**：无 TBD/TODO；所有测试与实现代码完整给出（Task 4 示例代码的布局微调是示例性质，非核心路径）。
- **类型一致性**：`ItemListState.sel_style` 在 Builder（build 时存入）与 add_item（克隆取出）间一致；`KeyOutcome::NavSelect(i32)` 在 on_key（产生）与 apply_key_outcome（消费）间一致；`item_base_style()`/`transparent()`/`column_layout()`/`default_sel_style()` 均为 itemlist.rs 内函数，跨 Step 引用一致。
- **测试数值**：`ensure_visible_scrolls_content` 的断言（item3 abs y = 20，item0 = -40）基于视口 40 高、item 20 高、内容 80 高的确定布局；`viewport_clips_scrolled_items` 用白色块背景做像素断言避免依赖文本渲染细节。
